// 不使用git notes
对于事务锁，可以不用事务队列，WATCH的键监控时,先默认要加键的读锁，MULTI收集命令时要检测键是否在WATCH里，并且键要加写锁，如果符合，则修改WATCH的键的锁类型
EXEC时先检测watched_dirty_标志，然后逐一在每个分片里加对应的锁，如果加锁失败，也就是读写冲突，可以判断事务失败，再释放锁，如果加锁成功，检测watched_dirty_标志，没有过期，继续一个一个分片试，
直到所有WATCH的键加上锁，再检测watched_dirty_，这里对于命令，只有WATCH的键加锁，其他的命令没有需求
emm, 普通的set也要加锁啊，有问题