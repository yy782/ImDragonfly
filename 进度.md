# ImDragonfly 工作背景与进度

## 一、项目背景

- **作者**：大学生（个人学习项目）。
- **项目**：`imdragonfly`，参考 `dragonflydb` 编写的仿制品。
- **现状问题**：部分源码是**从 dragonflydb 原版删减而来**（保留了原版头文件结构与版权声明，LICENSE 为 **BSL 1.1**）。
- **核心目标**：**去掉对 dragonflydb 源码的依赖**——以论文为行为依据，**重新实现一遍**调度链相关代码。行为语义从论文中来，不从原版代码中抄；每行带原版痕迹的代码都是待替换的"欠账"。
- **方法论约束**（用户反复强调）：
  1. **以论文为准**：先读论文，再动代码；直接参考原版源码没有意义（原版就是依赖本身）。
  2. **论文里有什么才写什么**：论文没有的功能不要凭空加（例：`dash_table` 的持久化逻辑就是论文有、源码没有，靠论文才写出来）。
  3. **不编译**：框架整理好之前禁止编译验证，一切改动停留在代码/框架层面。

## 二、已完成工作

### 1. 论文分析（VVL）

- 完整阅读 Kun Ren 等 *VLDB Journal 2015* 论文（文献位于 `docs/multi_shard_transaction.md` 最底部）。
- 核心机制梳理：
  - **计数器锁 `(CX, CS)`**：锁信息与数据共置，不再维护"谁等待谁"，锁状态一次内存访问即可获得；
  - **全局事务序 `TxnQueue`**：以 FIFO 全局顺序回答"锁释放后谁获得锁"，替代等待队列追踪；
  - **SCA（选择性冲突分析）**：从队首扫描、以位数组判断冲突，把可提前执行的事务从队首阻塞中解放出来；
  - **队首引理**：任何时刻只需检查队首即可推进调度。

### 2. 独立重写 / 实现（论文依据，脱离原版依赖）

- **`dash_table`**：已按论文重新实现（含论文有、源码没有的持久化逻辑）。
- **`db_table`**：已实现。
- **`expire_index`**：已实现。
- **`shard_storage`**：已实现。
- **`tx_queue`**：**不是原版删减的**（自研），无需重写。

### 3. 源码对照分析（VVL 论文 ↔ imdragonfly 实现）

逐条对应结论：

| 论文机制 | imdragonfly 实现 |
| --- | --- |
| 计数器对 `(CX, CS)` | `detail/intent_lock.hpp` 的 `IntentLock`（`Acquire` 授予判断与论文图 1 一致） |
| 全局事务序 TxnQueue | `txq_`（TxQueue，按 `txid` 有序插入） |
| 许可/就绪闸门 | `Transaction::is_armed` + `DisarmInShard` |
| 队首引理 | `EngineShard::PollExecution` 的队首循环 |
| SCA | `EngineShard::MaybeScheduleOutOfOrder`（新实现） |

### 4. 缺口分析与处理

- **缺口 1（`committed_txid_` 乱序执行时被赋值打回）**：用户判定**不是问题**，源码能正常运行、性能很好、无多分片协调问题。**已放弃，不再关注**。
- **缺口 2（队首 break 后调度停滞、乱序执行面太窄）**：已实现 —— `EngineShard::MaybeScheduleOutOfOrder`（SCA 落地，位数组冲突检测 + `kScgMaxScan` 扫描上限，仅队列超过高水位 `kQueueHighWater` 时激活）。
- **缺口 3（多分片事务锁不全时队列堆积、无界增长）**：已处理 —— `ScheduleInShard` 中多分片事务未拿到全部锁且队列达到 `kQueueHighWater` 时拒绝入队（VVL 论文 §2.1 语义）。

### 5. 调度质量评估结论

**源码调度骨架已足够优秀**，VVL 论文四个要件齐全：计数器锁、FIFO 全局序、armed 许可闸门、队首引理；加上 SCA 后，论文的调度推进机制基本闭环。剩余短板在"表达层"而非逻辑层。

## 三、当前任务（进行中）

**以论文语义重新实现 `engine_shard` 与 `transaction`，去除原版删减痕迹。**

- 这两个文件是**原版删减残留**（`engine_shard.cpp/hpp`、`transaction_layer/transaction.hpp/cpp`），保留了大量原版头文件结构、注释口吻与删除残留（如被注释的状态位、语义漂移的注释、冗余声明、不一致的索引约定等）。

### 关键决策（2026-08-24）：重写为**独立的类设计，接口不兼容**

- **接口不需要兼容**：上层建筑（command 层、回调层、pipeline 等）后续都要重写，现在兼容接口等于白做，兼容了和原版没有多大区别。
- **不兼容 ⇒ 必然编译不过 ⇒ 不要编译**：重写完成后不编译、不验证，直到上层建筑同步重写完毕。
- **定位**：不是"替换原版文件"，而是**设计一个全新的、独立的类**——从 VVL 论文语义出发，自由定义接口、结构、命名与状态机，不迁就任何原版调用点。
- 行为语义仍锚定 VVL 论文：计数器锁、txid 全局序、armed 许可、队首引理 + SCA、abort 重试。

## 四、待确认事项

- 重写范围：除 `engine_shard`、`transaction` 外，调度链上还有哪些文件属于原版删减（`intent_lock`、`db_table` 锁表等），需要逐个确认后纳入重写清单。
- 重写完成前：**不编译**。
- 独立设计后，上层建筑（`cmd_support`、`pipeline_squasher`、`multi_family` 等调用点）的重写另行排期。

## 五、engine_shard / transaction 独立重写完成（2026-08-24）

四个文件已按 VVL 论文语义重写为全新独立设计（接口不兼容原版，未编译）：
- `src/sharding/engine_shard.hpp` / `engine_shard.cpp`
- `src/transaction_layer/transaction.hpp` / `transaction.cpp`

### 设计要点

**transaction（协调器视角）**
- `ShardState` 原子位集：`kAllowed`（许可闸门，协调器写/分片线程清除）、`kKeyHeld`（持锁占位）、`kUncontended`（锁无竞争）、`kRanInline`（乐观内联已执行，重试防重复）。
- `ApplyForLockOn`：计数器锁申请（`Acquire` 无条件递增并判定授予）+ 乐观内联路径 + 入队；`LockResult` = kGranted / kQueued / kRejected（序冲突、高水位）。
- `Run`：fire-and-forget `cppcoro::AsyncTask` + 调用方 `resume_handle_`，全部执行完毕后恢复调用方协程；`Schedule`（多分片可重试）+ `Distribute`（放行 + 投递队列驱动）+ `barrier_` 集合点。
- 锁对称性：`Acquire` 失败也递增，`ReleaseLocks` 无条件对称递减。

**engine_shard（分片线程视角）**
- `DriveQueue`（队首引理）：清除 `kAllowed` → 队首循环放行执行 → 队首未就绪时 SCA 乱序扫描。
- `MaybeScheduleOutOfOrder`（SCA）：位数组写集/读集冲突检测，从队首扫描无冲突事务提前执行。
- `ShardLock`（全局计数器锁）、`committed_txid_` 推进、`OptimisticBehavior`。

### 键遍历替代方案（原版 Slice 拆分）

原版 `LockFpSlice`（键视图 + 数据访问耦合）拆为三个正交接口：
- `SliceOn(sid) -> DbSlice&`：数据访问（`ns_->GetDbSlice(sid)`）；
- `KeysOn(sid) -> span<const std::string_view>`：键视图，按 `ShardState.key_begin/key_count` 分段切出，可 range-for 遍历；
- `LockArgsOn(sid) -> KeyLockArgs`：锁参数（键 + 指纹 + 配置）。

### 关键修正记录

- `DriveQueue` 队首引理 bug：第 2 步队首循环在 `head == tx` 时必须无条件放行（事务自身已是队首，无前置阻塞），否则乱序事务会卡死。
- 单分片路径：`Run` 预置 `kAllowed`，`ApplyForLockOn` 不再清除；`AllowOnIf` 仅在 `head->flags & kAllowed == 0`（无协调器放行竞争）时调用。
- include 路径：`util/cppcoro/async_task.hpp`（`src` 为 include 根；`AsyncTask` 为 fire-and-forget，`final_suspend = never` 自动销毁帧，不可 `co_await`）。

### 已确认依赖接口

`BlockingCounter`/`EmbeddedBlockingCounter`（Start/Add/Dec/Wait/GetCount）、`util/cppcoro/task.hpp`、`Namespace::GetDbSlice(ShardId)`、`EngineShard::GetDbSlice/ShardLock/CommittedTxId`、`TxQueue`（push/pop/Iterator）、`IntentLock`、`KeyLockArgs`。

### 状态

- 全量逻辑自查完成（队首引理、SCA 与锁对称性、pending 归零 + `resume_requested_`/`complete_` 双重检查保证 resume 恰好一次）。
- 按约定**不编译**；上层建筑（`cmd_support` 等）适配另行排期。

## 六、修改原则（2026-08-24，用户明确）

- **代码修改不追求完整，追求大体正确即可**。
- 原因：要改的地方太多，改动是连锁的。例如事务逻辑依赖 `CommandId` 接口，目前是按现有 `CommandId` 写的；但 `CommandId` 后续也要重写，届时事务逻辑必然跟着改。既然接口层都在变动，现在追求"一次性写对"没有意义。
- 结论：每个文件/类达到"当前接口下的正确实现"即视为完成，不做超前适配；后续接口变动时再增量修改。


### 边界原则（用户明确）
- 重写类不需要动别的文件，因为别的文件也要重写，你适配没有意义，此外，不要做验证include环，我们不需要编译，你验证有什么意义




- 关于某个类型的修改,你不能根据类型的引用而觉得要不要动手脚，这是不动的，因为我们在重构，重写

## 七、命令注册体系重写（表驱动注册，2026-08-24）

原版命令注册依赖 `CommandId` 的链式/函数指针接口（`SetHandler` 等），上层命令家族均基于它。已完成重写：

- **`CommandId`**（`command_layer/command_id.hpp/cpp`）：已重写为全新接口。
- **`CommandRegistry`**（`command_layer/command_registry.hpp/cpp`）：改为**表驱动注册**——命令目录表（名字 → 注册函数指针），注册函数内构造 `CommandId` 并注册。
- **`StringFamily` / `GenericFamily`**：删除原版 `using CI` 别名、`family_` 字段与 fluent 区，注册函数改为表驱动方式。
- **`ListFamily` / `HashFamily` / `SetFamily` / `ZSetFamily`**：注册接口已被注释（缩小改动面），待验证可行性后按同样方式跟进。

约定：不编译；接口层仍在变动，达到"当前接口下正确"即可。

## 八、移除 namespaces 多租户功能（2026-08-24）

`Namespace` / `Namespaces` 是早期为多租户数据隔离设计的实验功能（每 namespace 一套跨分片 `DbSlice` 池），已弃用并删除：

- 删除 `src/sharding/namespaces.hpp`、`src/sharding/namespaces.cpp`。
- `engine_shard_set.cpp`：删 `#include "namespaces.hpp"`；`Init()` 不再 `new Namespaces()`；`Shutdown()` 不再 `Clear()` / `delete`。

**遗留（未动，待统一清理）**：
- `detail/common_types.hpp`：`class Namespaces;` 前向声明 + `inline Namespaces* namespaces = nullptr;` 全局指针；
- `network/redis_server.hpp`：include + 2 处 `namespaces->GetDefaultNamespace()`；
- `command_layer/generic_family.cpp`：include + 1 处 `namespaces->GetDefaultNamespace()`（另含 DbSlice 残留，见第九节）；
- `persistence/rdb_serializer.cpp`：include + `ForEach` / `GetOrInsert` 各 1 处；
- `detail/tx_base.hpp`：仅注释掉的 include，无实际引用；
- CMake 编译列表若含 `namespaces.cpp` 需一并移除。

## 九、命令层从 DbSlice 迁移到 ShardStorage（2026-08-24）

第八节删掉 `Namespace` 后，`DbSlice`（由 `Namespace::GetDbSlice` 提供）一并消失，命令层 cb 的数据访问改为：**`es->GetShardStorage()` 拿存储 + `tx->GetDbContext()` 拿上下文**。接口不再有 `Namespace` 参与。

### shard_storage.hpp 接口定型

- 接口参数类型 `Context` 全部改为 **`DbContext`**（`detail/tx_base.hpp` 定义，`Transaction::GetDbContext()` 返回类型，两侧直接对上，cb 无需转换）。
- `OpResult` 确定组装方式：**`template <typename T> using OpResult = util::expected<T, OpStatus>`**（`util/expected.hpp` + `detail/op_status.hpp` 组装），删除残留的 `using facade::OpResult/OpStatus` 与不存在的 `#include "op_status.hpp"`。
- 错误返回统一 `util::make_unexpected(OpStatus::XXX)`；成功返回值或 `{}`；判定用 `has_value()` / `error()`（无 `operator*`、无 `status()`）。

### string_family.cpp 迁移（15 条命令全部完成）

各命令 cb 从 `DbSlice` 原语迁移到 `ShardStorage` 原语：

| 命令 | 旧（DbSlice） | 新（ShardStorage） |
| --- | --- | --- |
| SET | `AddOrFind`+`DelMutable`+`AddExpire`/`RemoveExpire` | `Exists`/`ExpireTime` + `Upsert`（值+TTL 一体提交） |
| MSET | `AddOrUpdate` | `Upsert` |
| MGET/GET/STRLEN/GETRANGE | `FindReadOnly` + `GetInnerIt()` | `Find` → `ValueView`（`res.value().value`） |
| INCR/DECR/INCRBY/DECRBY | `AddOrFind` + 手动读改写 | `Mutate`（原地修改，`IsEmpty()` 识别新建键） |
| APPEND/SETRANGE | `AddOrFind` | `Mutate` |
| SETNX | `AddOrFind` | `Exists` + `Upsert` |
| GETSET | `AddOrFind`+`RemoveExpire` | `Find` + `Upsert(ttl=0)`（清 TTL 语义保留） |
| GETDEL | `FindMutable`+`DelMutable` | `Find` + `Delete` |

- cb 返回类型统一为 `OpResult<T>`（SET 为 `OpResult<bool>`：error=底层错误 / `false`=NX/XX 未命中 / `true`=已设置）。
- 删除 `#include "sharding/db_slice.hpp"`，显式加 `detail/tx_base.hpp`（DbContext）。
- 封装函数按用户要求内联进 cb（例：SET 逻辑直接写在 cb 内，不单独包装）。
- lint 零错误；按约定不编译。

### 遗留

- `command_layer/generic_family.cpp`：仍有 DbSlice/Namespace 残留（`namespaces->GetDefaultNamespace()` 1 处 + 数据访问），待按同样方式迁移。

## 十、transaction 调度类型嵌套与键/指纹存储重构（2026-08-24）

### 1. 调度类型移入 Transaction 类（消除命名空间污染）

`ShardFlag` / `PerShardData` / `ScheduleResult` 原为 `dfly` 命名空间作用域定义，现全部嵌套进 `Transaction` 类：

| 类型 | 可见性 | 理由 |
| --- | --- | --- |
| `ShardFlag`（kShardInvolved / kUncontended / kRanInline） | public | 位常量是 `AllowOnIf` 的条件参数，engine_shard 引用 `Transaction::kUncontended` |
| `ScheduleResult`（kGranted / kQueued / kRejected） | public | `ScheduleOnShard` 公开接口的返回类型，必须对外可见 |
| `PerShardData` | private | 纯内部结构（`ShardDataAt` / `InvolvedData` / `ReleaseLocks` 使用），不外泄 |

- `engine_shard.cpp` 调用点改 `tx->AllowOnIf(sid, Transaction::kUncontended)`；
- `transaction.cpp` 成员函数定义中裸名（返回类型/参数/函数体）按标准在类作用域内解析，无需改动。

### 2. 键与指纹收进 PerShardData（删除全局平铺）

此前键段与指纹是"先按分片归堆、再逐片平铺"的一维数组（`args_slices_` / `kv_fp_`），`PerShardData` 只记段界（`slice_start/count`、`fp_start/count`），取锁/遍历时再切段——数据与视图分离、结构冗余。重构后：

```cpp
struct alignas(64) PerShardData {
  uint16_t flags = 0;
  std::atomic_bool is_armed = false;
  std::vector<IndexSlice> slices;   // 本分片键参数下标段（相邻键合并）
  std::vector<LockFp> fps;          // 与本分片键一一对应的锁指纹
  TxQueue::Iterator queue_pos = TxQueue::kEnd;
};
```

- **删除**：成员 `args_slices_` / `kv_fp_` / `key_slices_`、`PerShardData` 段界四字段、`BuildSliceViews()`；
- **`Slice` 降级**为按需现构造的轻量视图：`GetSlice(sid)` 改按值返回，基于 `sd.slices + key_step + args_` 现构造（新增构造函数），不再预存每分片视图数组；
- **消费方同步**：`LockArgsOn` → `res.fps = sd.fps`；`GetKeyNum` → 遍历各分片 fps 求和；`BuildKeyMap::fill` → `sd.slices = std::move(acc.slices)`，指纹直接算入 `sd.fps`；
- **move 构造**由 no-op 改为真正转移两个 vector（原子成员走默认构造）；
- `generic_family.cpp` 两处 `auto& slice` → `const auto& slice`（按值返回后绑定临时对象）。

收益：分片线程取锁（`LockArgsOn`）与遍历键（`GetSlice`）只接触自己那份数据，数据局部性更好；类内外命名更干净。

### 3. 调度链小修（同日）

- `Schedule` 回滚后驱动广播：`need_poll` 由 `bool` 改 `std::atomic<bool>`（分片线程并发写）；
- `AllowOnIf` 去掉冗余的 `!is_armed.load` 前置判断，直接用 `exchange(false, acq_rel)` 语义；
- `InvokeCallback` 由 `bool` 改 `void`（唯一调用点忽略返回值）。

### 状态

- 全仓库零残留引用（`args_slices_` / `kv_fp_` / `key_slices_` / `BuildSliceViews` / 段界字段），lint 零错误；
- 按约定不编译；上层建筑（命令注册、回调层）适配另行排期。